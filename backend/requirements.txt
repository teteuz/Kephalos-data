# ===========================================
# KephalosData Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0
flask-limiter>=3.5.0
gunicorn>=21.0.0

# ============= LLM =============
# OpenAI SDK (unified OpenAI format for LLM calls)
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.13.0

# ============= OASIS Social Simulation =============
# OASIS social simulation framework
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Stripe + Supabase (auth & subscriptions) =============
stripe>=10.0.0
supabase>=2.0.0

# ============= Utility Libraries =============
# Environment variable loading
python-dotenv>=1.0.0
